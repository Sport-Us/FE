export const metadata = {
  title: "Sport:Us - 로그인",
  description: "Sport:Us 로그인 페이지입니다.",
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
